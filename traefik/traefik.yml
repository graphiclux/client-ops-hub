api:
  dashboard: true
entryPoints:
  web:
    address: ":80"
  websecure:
    address: ":443"
providers:
  docker:
    exposedByDefault: false
certificatesResolvers:
  letsencrypt:
    acme:
      tlsChallenge: {}
      email: ops@graphiclux.com
      storage: /letsencrypt/acme.json
